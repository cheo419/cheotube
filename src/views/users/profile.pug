extends ../base
include ../mixins/video

block content
    div.profile__data
        if loggedInUser.avatarUrl === undefind
            span.header__avatar2 😀
        else if loggedInUser.avatarUrl[0] === "h"
            img.profile__data(src="" + loggedInUser.avatarUrl)
        else
            img.profile__data(src="/" + loggedInUser.avatarUrl)
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found